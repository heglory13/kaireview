# Dokploy / production environment template
# Replace the host with your real server IP or domain if needed.

APP_HOST_PORT=3001
HOSTNAME=0.0.0.0

# Admin credentials for the review dashboard
KAI_ADMIN_USERNAME=admin
KAI_ADMIN_PASSWORD=change-this-password
KAI_ADMIN_SESSION_SECRET=change-this-to-a-long-random-secret
